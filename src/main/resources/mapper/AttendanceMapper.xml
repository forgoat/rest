<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rest.mapper.AttendanceMapper">
    <select id="findAttendanceByKlassSeminarId" parameterType="long" resultType="com.rest.po.Attendance">
        SELECT * FROM standard_seminar_system.attendance WHERE klass_seminar_id=#{klassSeminarId}
    </select>
    <insert id="saveAttendance" parameterType="com.rest.po.Attendance" useGeneratedKeys="true" keyProperty="id">
         INSERT INTO standard_seminar_system.attendance(klass_seminar_id,team_id,team_order,is_present)VALUE(#{klassSeminarId},#{teamId},#{teamOrder},0)
    </insert>

    <delete id="deleteById" parameterType="long">
        DELETE FROM standard_seminar_system.attendance WHERE id=#{attendanceId}
    </delete>
    <update id="updateAttendanceStatus">
        UPDATE standard_seminar_system.attendance SET is_present=#{status} WHERE id=#{attendanceId}
    </update>
    <update id="updateAttendance" parameterType="Long">
        UPDATE standard_seminar_system.attendance
        SET
        ppt_name=#{pptName},
        ppt_url=#{pptUrl}
        WHERE
        id=#{id}
    </update>
    <select id="queryByKlassSeminarIdAndTeamId" resultType="com.rest.po.Attendance">
        SELECT *
        FROM standard_seminar_system.attendance
        WHERE
        klass_seminar_id=#{klassSeminarId}
        AND
        team_id=#{teamId}
    </select>

    <select id="queryAttendanceById" resultType="com.rest.po.Attendance">
        SELECT *
        FROM standard_seminar_system.attendance
        WHERE
        id=#{id}
    </select>
</mapper>